-- Official copilot.vim configuration
-- Disable default Tab mapping and set custom accept/dismiss keys.

-- Prevent plugin from mapping Tab to accept suggestions (avoid cmp conflicts)
-- This file is no longer used; copilot.lua + copilot-cmp is used instead.
-- Left as a placeholder in case you want to revert to the official plugin later.
return {}
